# ============================================================
# Brücken Upload Portal — app-brucken-upload
# App Service separado do motor de crédito
# ============================================================

# Azure Storage — bruckencredito
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=bruckencredito;AccountKey=SUA_KEY;EndpointSuffix=core.windows.net
AZURE_STORAGE_CONTAINER=uploads-clientes

# SQL Server — srv-credito-analytics / db_credito
# (mesmas credenciais do credito-app-brucken)
DB_SERVER=srv-credito-analytics.database.windows.net
DB_NAME=db_credito
DB_USER=SEU_ADMIN_USER
DB_PASSWORD=SUA_SENHA

# Auth
JWT_SECRET=GERE_UMA_CHAVE_AQUI

# CORS
ALLOWED_ORIGINS=https://app-brucken-upload.azurewebsites.net

# Flask (apenas dev local)
FLASK_DEBUG=true
